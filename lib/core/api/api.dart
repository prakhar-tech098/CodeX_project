import 'package:flutter/cupertino.dart';

class Api_auth extends StatelessWidget {
  const Api_auth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
